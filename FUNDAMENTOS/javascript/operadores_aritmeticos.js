const n1 = 90
const n2 = 150
const soma = n1 + n2    // operador de soma
const subt = n2 - n1    // operador de subtração
const mult = n1 * 2     // operador de multiplicação
const divs = n1 / 3     // operador de divisão
const mod = n1 % n2     // operador para módulo retorna o resto da divisão
const potencia = 8 ** 2 // operador de calcular potência

// função para retornar uma resultado de uma operação no console.log
function result(op) {
 console.log(op)
}

// utilizando a função
result(soma)
result(subt)
result(mult)
result(divs)
result(mod)
result(potencia)